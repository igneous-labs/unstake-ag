{
  "extends": "./tsconfig.esm.json",
  "compilerOptions": {
    "types": ["node", "mocha"],
    "rootDir": "."
  },
  "include": ["src/**/*.ts", "tests/**/*.ts", "scripts/**/*.ts"],
  "exclude": ["node_modules", "dist"]
}
